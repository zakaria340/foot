{% extends 'base.html.twig' %}

{% block body %}
    <table>
        <tbody>
        {% for key, user in users %}
            <tr>
                <td>
                    {{ user.username }}
                </td>
                <td class="foreignerCountry">
                    {{ user.points }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
